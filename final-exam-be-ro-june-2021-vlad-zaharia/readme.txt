To start the project:
 - make sure to add config.env file into ./config/config.env
 - npm start will start the api(Be in the right folder)
 - you can also run node seeder.js from utils/seeder.js to import some mock data
 - switch to the react app website and run yarn start and go to localhost:5000 and play around
